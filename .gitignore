node_modules/

# Credentials never belong in the repo. Adapters read them from the
# environment; only the example file is tracked.
.env
.env.*
!.env.example

# Raw model responses. Kept out by default so a run cannot accidentally
# publish anything; a curated baseline is committed under baselines/ instead.
runs/

# Generated reports. One example lives in docs/ instead.
reports/
